specVersion: "v1beta"
serviceId: "ldp2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ldp2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
